version: '3.5'

services:

  redis-dev:
    container_name: redis-db
    image: redis:alpine3.18
    restart: always
    ports:
      - "6379:6379"
      - "8001:8001"
    volumes:
      - /volumes/redis:/data
    networks:
      - bacsystem

networks:
  bacsystem:
    driver: bridge